import { Autowired } from '@exteranto/ioc'
import { Versioning } from '@exteranto/compatibility'

class SomeService {

  @Autowired
  private versioning: Versioning

  public execute () : void {
    this.versioning.until('2.0.0', () => {
      return 1
    }).then((result) => {
      // if app.version <= 2.0.0, assert result === 1
      // else assert result === undefined
    })
  }
}
