import { Pipeline } from '@exteranto/support'

new Pipeline().send(Promise.resolve('test'))
  .via('handle')
  .through([
    { handle: passable => passable + '1' },
    { handle: async (passable) => passable + '2' },
    { handle: passable => passable + '3' }
  ])
  .then((result) => {
    // assert result === 'test123'
  })
