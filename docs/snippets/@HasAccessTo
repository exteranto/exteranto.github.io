import { HasAccessTo, Permission } from '@exteranto/management'

export class CustomService {

  @HasAccessTo(Permission.DOWNLOADS)
  public listDownloads () : Promise<any> {
    // This method only executes if the extension has
    // permissions to "downloads" APIs.
    // If this permission is not granted, the method returns
    // rejected Promise with PermissionNotGrantedException.
  }

  @HasAccessTo([Permission.BOOKMARKS, Permission.ACTIVE_TAB])
  public listBookmarksInActiveTab () : Promise<any> {
    // You can send an array of permissions that determine
    // whether this method can be executed.
  }
}
