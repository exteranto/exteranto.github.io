import { Autowired } from '@exteranto/ioc'
import { VersionResolver } from '@exteranto/compatibility'

class SomeService {

  @Autowired
  private versionResolver: VersionResolver

  public execute () : void {
    const result = this.versionResolver.higher('2.0.0', '1.0.0')

    // assert result === true
  }
}
