import { WiredWith } from '@exteranto/ioc'
import { Storage } from '@exteranto/storage'

export class MyService {

  @WiredWith(['sync'])
  private storage: Storage

  public execute () : void {
    this.storage.all()
      .then((data) => {
        // Do anything after the data is retrieved, 'data' is a big object of
        // key-value pairs.
      })
  }
}
